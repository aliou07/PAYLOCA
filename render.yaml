services:
  - type: web
    name: payloca-frontend
    env: static
    rootDir: artifacts/niger-habitat
    buildCommand: pnpm install --frozen-lockfile && pnpm run build
    staticPublishPath: dist/public
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
