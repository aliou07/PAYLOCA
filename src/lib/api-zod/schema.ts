/**
 * Schémas Zod générés à partir du contrat OpenAPI PAYLOCA.
 *
 * Les schémas complets sont générés automatiquement dans api-zod/src/generated.
 * Cette ré-exportation conserve tous les endpoints, validations et contrats
 * existants sans dupliquer le code généré.
 */
export * from "./src/generated/api";
